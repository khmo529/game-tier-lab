name: Tier Update (Weekly)

on:
  schedule:
    - cron: '0 3 * * 1' # 매주 월요일 12시 KST (03:00 UTC)
  workflow_dispatch:

concurrency:
  group: tier-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: pip install requests beautifulsoup4 lxml

      # FIX: working-directory 지정으로 data/characters.json 경로 문제 해결
      - name: Run BD2 updater
        working-directory: games/browndust2
        run: python tier_updater.py

      - name: Run NIKKE updater
        working-directory: games/nikke
        run: python tier_updater.py

      - name: Sync JSON + Images to theme
        run: |
          mkdir -p wp-content/themes/generatepress-child/browndust2-tier/data
          mkdir -p wp-content/themes/generatepress-child/nikke-tier/data
          mkdir -p wp-content/themes/generatepress-child/browndust2-tier/assets/images
          cp games/browndust2/data/*.json wp-content/themes/generatepress-child/browndust2-tier/data/ || true
          cp games/nikke/data/*.json wp-content/themes/generatepress-child/nikke-tier/data/ || true
          # 이미지도 같이 동기화 (기존 누락 원인)
          cp games/browndust2/assets/images/* wp-content/themes/generatepress-child/browndust2-tier/assets/images/ 2>/dev/null || true

      - name: Commit & Push (rebase safe)
        run: |
          git config user.name "tier-bot"
          git config user.email "bot@nopickle.co.kr"
          git add -A
          if git diff --staged --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "chore: weekly tier update $(date +%Y-%m-%d)"
          git pull --rebase origin main
          git push origin main
